export function SectionHeading({ children }) {
  return (
    <div className="border-b-brutal pb-4 mb-10 w-full">
      <h2 className="font-display text-4xl md:text-6xl uppercase tracking-tighter">{children}</h2>
    </div>
  )
}
